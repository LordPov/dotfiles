#!/bin/bash

echo "select deployment_ as Deployment,component_ as Component,revision_ as Image from component_ where deleted_ is null and component_ like '%$1%' and (deployment_ like 'u%' or deployment_ like 'p%') order by deployment_,component_" | mysql -h apddb -u reader -preader deployments

